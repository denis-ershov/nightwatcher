# Варианты запуска NightWatcher

NightWatcher можно запустить несколькими способами. Все варианты запускают одновременно API сервер и Watcher.

## 🚀 Вариант 1: Единый Python скрипт (Рекомендуется)

Самый простой способ - использовать единый скрипт `run.py`:

```bash
python run.py
```

**Преимущества:**
- ✅ Простота использования
- ✅ Один процесс управляет обоими сервисами
- ✅ Корректное завершение при Ctrl+C
- ✅ Работает на Windows, Linux, macOS

**Недостатки:**
- ⚠️ Если один процесс упадет, нужно перезапускать оба

---

## 🐳 Вариант 2: Docker Compose

Используйте Docker для изолированного запуска:

```bash
# Сборка образа
docker-compose build

# Запуск
docker-compose up

# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down
```

**Преимущества:**
- ✅ Изоляция окружения
- ✅ Легкое развертывание на любом сервере
- ✅ Автоматический перезапуск при сбоях
- ✅ Управление через docker-compose

**Недостатки:**
- ⚠️ Требует установки Docker
- ⚠️ Больше потребление ресурсов

---

## 🔧 Вариант 3: Makefile

Используйте Make для удобного управления:

```bash
# Установка зависимостей
make install

# Запуск обоих сервисов
make run-all

# Запуск только API
make run-api

# Запуск только Watcher
make run-watcher

# Docker команды
make docker-build
make docker-run
make docker-stop
```

**Преимущества:**
- ✅ Стандартный инструмент для разработки
- ✅ Множество команд в одном месте
- ✅ Удобно для разработки

**Недостатки:**
- ⚠️ На Windows требует установки Make или WSL

---

## 🪟 Вариант 4: Batch скрипт (Windows)

Для Windows можно использовать `run_all.bat`:

```cmd
run_all.bat
```

Или двойной клик по файлу в проводнике.

**Преимущества:**
- ✅ Простота для Windows пользователей
- ✅ Автоматическая активация venv
- ✅ Не требует установки дополнительных инструментов

---

## 🔄 Вариант 5: Supervisor (Linux)

Для production на Linux используйте Supervisor:

```bash
# Установка Supervisor
sudo apt-get install supervisor

# Копирование конфигурации
sudo cp supervisor.conf /etc/supervisor/conf.d/nightwatcher.conf

# Обновление конфигурации (укажите правильные пути)
sudo nano /etc/supervisor/conf.d/nightwatcher.conf

# Перезагрузка Supervisor
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start nightwatcher-api
sudo supervisorctl start nightwatcher-watcher
```

**Преимущества:**
- ✅ Автоматический перезапуск при сбоях
- ✅ Логирование в отдельные файлы
- ✅ Управление через supervisorctl
- ✅ Запуск при старте системы

**Недостатки:**
- ⚠️ Только для Linux
- ⚠️ Требует настройки путей

---

## ⚙️ Вариант 6: Systemd (Linux)

Для systemd-based систем (Ubuntu 16+, Debian 8+):

```bash
# Копирование сервиса
sudo cp systemd-nightwatcher.service /etc/systemd/system/nightwatcher.service

# Редактирование путей
sudo nano /etc/systemd/system/nightwatcher.service

# Включение автозапуска
sudo systemctl daemon-reload
sudo systemctl enable nightwatcher
sudo systemctl start nightwatcher

# Проверка статуса
sudo systemctl status nightwatcher
```

**Преимущества:**
- ✅ Интеграция с системой
- ✅ Автозапуск при загрузке
- ✅ Управление через systemctl
- ✅ Логирование через journalctl

**Недостатки:**
- ⚠️ Только для Linux с systemd
- ⚠️ Требует настройки путей

---

## 📋 Сравнение вариантов

| Вариант | Простота | Production | Windows | Linux | macOS |
|---------|----------|------------|---------|-------|-------|
| Python скрипт | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ | ✅ | ✅ |
| Docker Compose | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ✅ | ✅ |
| Makefile | ⭐⭐⭐⭐ | ⭐⭐ | ⚠️ | ✅ | ✅ |
| Batch скрипт | ⭐⭐⭐⭐⭐ | ⭐⭐ | ✅ | ❌ | ❌ |
| Supervisor | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ | ✅ | ❌ |
| Systemd | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ | ✅ | ❌ |

---

## 🎯 Рекомендации

- **Для разработки:** Используйте `python run.py` или `make run-all`
- **Для тестирования:** Используйте Docker Compose
- **Для production на Linux:** Используйте Supervisor или Systemd
- **Для Windows production:** Используйте Docker или Task Scheduler с `run.py`

---

## 🔍 Отдельный запуск сервисов

Если нужно запустить только один сервис:

```bash
# Только API сервер
python -m uvicorn app.api:app --host 127.0.0.1 --port 8000 --reload

# Только Watcher
python run_watcher.py
```
